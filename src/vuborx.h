#if !defined VUBOR_X_H
#define VUBOR_X_H
#include <stdio.h>
#include <stdlib.h>
#include <windows.h>
#include <stdbool.h>

bool in_range (char* buff, const char* max);
bool input(const char* title, int a);
void vuborx(int a);

#endif
